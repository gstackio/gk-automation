---
platform: linux

inputs:
  - name: repo          # type: git
  - name: gk-automation # type: git

outputs:
  - name: repo-committed

params:
  GIT_COMMIT_NAME:
  GIT_COMMIT_EMAIL:
  GIT_COMMIT_MESSAGE:

run:
  path: gk-automation/boshrelease/git-push.sh
